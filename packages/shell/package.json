{
  "name": "@moke/shell",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "build:native:macos": "cargo build --release --manifest-path native/macos-sandbox/Cargo.toml",
    "build:native": "node scripts/build-native.mjs",
    "build:native:windows": "cargo build --release --manifest-path native/windows-sandbox/Cargo.toml",
    "clean": "tsc -b --clean"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"]
}
